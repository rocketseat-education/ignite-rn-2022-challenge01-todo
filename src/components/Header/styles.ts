import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    paddingVertical: 70,
    backgroundColor: '#0D0D0D',
    alignItems: "center",
  }
})